if(x>3){
  print("hello")
}
x <- rnorm(100)
d <- list(1,2,3)
hist(x)

set.seed(5)
x <- sample(1:8,size=4,replace=FALSE)

------
probs = c(5,5,5,5,1,1,1,1)/16
sample(1:8,size=4,replace=FALSE,prob=probs)
-----
  set.seed(31);
heightsCM = rnorm(30,mean=188, sd=5);
weightsK = rnorm(30,mean=84,sd=3); 
hasDaughter = sample(c(TRU[E,FALSE),size=30,replace=T); 
dataFrame = data.frame(heightsCM,weightsK,hasDaughter); 

dataFrameSubset = dataFrame[heightsCM<180,]
mean(dataFrameSubset$weightsK)